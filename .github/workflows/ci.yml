name: CI

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: package-lock.json
      - name: Install locked dependencies
        run: npm ci
      - name: Typecheck workspaces
        run: npm run typecheck
      - name: Test calculator
        run: npm --workspace @40k-calculator/calculator run test
      - name: Test game data schema
        run: npm --workspace @40k-calculator/game-data-schema run test
      - name: Test web application
        run: npm --workspace @40k-calculator/web run test
      - name: Verify data release
        run: npm run verify:data-release
      - name: Build web application
        run: npm run build
